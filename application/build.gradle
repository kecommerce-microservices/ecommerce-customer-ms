plugins {
    id 'java-library'
    id 'java-conventions'
}

group = 'com.kaua.ecommerce.customer.application'

dependencies {
    implementation(project(':domain'))

    testImplementation(project(path: ':domain', configuration: 'testClasses'))
}